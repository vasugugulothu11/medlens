# Google Gemini API key for lab extraction, patient-friendly summary, and inconsistency detection
GOOGLE_API_KEY=YOUR_GEMINI_API_KEY

# Database connection URL (defaults to local SQLite for hackathons, swap to PostgreSQL for production)
# Example PostgreSQL: postgresql://user:password@localhost:5432/medlens_db
DATABASE_URL=sqlite:///./medlens.db
