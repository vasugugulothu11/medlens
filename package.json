{
  "name": "medlens-workspace",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix backend/node-express run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend/node-express run dev",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend/node-express run build",
    "build": "npm run build:frontend"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "@vercel/node": "^5.1.10",
    "typescript": "~5.8.2"
  }
}
